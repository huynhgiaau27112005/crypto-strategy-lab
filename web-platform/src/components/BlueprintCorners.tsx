/**
 * The four registration-mark corners the prototype pairs with `.blueprint`
 * (e.g. `<i class="corner tl">…`). Styling lives in styles/global.css.
 */
export default function BlueprintCorners() {
  return (
    <>
      <i className="corner tl" />
      <i className="corner tr" />
      <i className="corner bl" />
      <i className="corner br" />
    </>
  )
}
